import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'griddle.crunch.clientside',
  appName: 'griddle-crunch-clientside',
  webDir: 'dist'
};

export default config;
